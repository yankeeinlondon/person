import { isArray } from "src/runtime/index";
/**
 * **isLeftRight**(value)
 *
 * Type guard for detecting a `LeftRight` result.
 */
export const isLeftRight = (value) => {
    return isArray(value) && value.length === 3 && value[0] === "LeftRight";
};
