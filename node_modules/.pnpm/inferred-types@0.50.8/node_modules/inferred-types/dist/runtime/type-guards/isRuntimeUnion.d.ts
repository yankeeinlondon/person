import { RuntimeUnion } from "src/types/index";
/**
 * **isRuntimeUnion**
 *
 * A type guard which checks whether the passed in value is a `RuntimeUnion`.
 */
export declare const isRuntimeUnion: <T extends unknown[]>(val: unknown) => val is RuntimeUnion<T>;
//# sourceMappingURL=isRuntimeUnion.d.ts.map