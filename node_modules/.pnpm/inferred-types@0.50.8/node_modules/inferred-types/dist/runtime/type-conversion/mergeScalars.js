import { isUndefined } from "../type-guards/isUndefined";
/**
 * **mergeScalars**(a,b)
 *
 * The value of scalar `b` _overrides_ that of `a` unless `b` is
 * undefined.
 */
export function mergeScalars(a, b) {
    return (isUndefined(b) ? a : b);
}
