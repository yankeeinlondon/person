import type { IsArray, Narrowable } from "src/types/index";
/**
 * **ifArray**(T, IF, ELSE)
 *
 * A utility which evaluates a type `T` for whether it is an array and then
 */
export declare function ifArray<T extends Narrowable, IF extends Narrowable, ELSE extends Narrowable>(val: T, isAnArray: <N extends T & unknown[]>(arr: N) => IF, isNotAnArray: <N extends Exclude<T, unknown[] | readonly unknown[]>>(nonArr: N) => ELSE): IsArray<T> extends true ? IF : ELSE;
//# sourceMappingURL=ifArray.d.ts.map