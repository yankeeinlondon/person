import { EnsureTrailing } from "src/types/index";
/**
 * **ensureTrailing**(content, strip)
 *
 * Runtime utility which ensures that last part of a string -- `content` -- has the
 * substring `ensure` at the end and adds it if not present.
 */
export declare function ensureTrailing<T extends string, U extends string>(content: T, ensure: U): EnsureTrailing<T, U>;
//# sourceMappingURL=ensureTrailing.d.ts.map