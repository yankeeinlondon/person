import { Narrowable, TupleToUnion } from "src/types/index";
/**
 * **union**(options) => (value)
 *
 * A higher order function which creates a union type with a runtime value set.
 *
 * **Related:** `unionize`
 */
export declare const union: <TOptions extends readonly Narrowable[]>(...options: TOptions) => <TValue extends TupleToUnion<TOptions>>(value: TValue) => TupleToUnion<TOptions>;
//# sourceMappingURL=union.d.ts.map