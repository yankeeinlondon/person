/**
 * **cssColor**`(colorspace, v1, v2, v3, [opacity]) -> CssColorFn`
 *
 * Produces a string literal for a CSS v4 `color()` function.
 */
export const cssColor = (color, v1, v2, v3, opacity) => {
    return `color(${color} ${v1} ${v2} ${v3}${opacity ? ` / ${opacity}` : ""}`;
};
