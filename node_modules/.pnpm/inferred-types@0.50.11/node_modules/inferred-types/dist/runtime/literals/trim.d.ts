import { Trim, TrimLeft, TrimRight } from "src/types/index";
/**
 * **trim**(input)
 *
 * A runtime utility which trims whitespace on both sides of a string
 * and returns a type-strong string literal where possible.
 */
export declare function trim<T extends string>(input: T): Trim<T>;
/**
 * **trimLeft**(input)
 *
 * A runtime utility which trims whitespace on the leading/left side of a string
 * and returns a type-strong string literal where possible.
 *
 * @deprecated prefer use of `trimStart` instead
 */
export declare function trimLeft<T extends string>(input: T): TrimLeft<T>;
/**
 * **trimStart**(input)
 *
 * A runtime utility which trims whitespace on the leading/left side of a string
 * and returns a type-strong string literal where possible.
 */
export declare function trimStart<T extends string>(input: T): TrimLeft<T>;
/**
 * **trimRight**(input)
 *
 * A runtime utility which trims whitespace on the tailing/right side of a string
 * and returns a type-strong string literal where possible.
 *
 * @deprecated prefer use of `trimEnd` instead
 */
export declare function trimRight<T extends string>(input: T): TrimRight<T>;
/**
 * **trimEnd**(input)
 *
 * A runtime utility which trims whitespace on the tailing/right side of a string
 * and returns a type-strong string literal where possible.
 */
export declare function trimEnd<T extends string>(input: T): TrimRight<T>;
//# sourceMappingURL=trim.d.ts.map