import { Dictionary, IsObject } from "src/types/index";
type ContainerType<T> = T extends any[] ? any[] : IsObject<T> extends true ? Dictionary : T extends Map<any, any> ? Map<any, any> : T extends Set<any> ? Set<any> : T extends WeakMap<any, any> ? WeakMap<any, any> : object;
export declare function isContainer<T>(value: T): value is T & ContainerType<T>;
export {};
//# sourceMappingURL=isContainer.d.ts.map