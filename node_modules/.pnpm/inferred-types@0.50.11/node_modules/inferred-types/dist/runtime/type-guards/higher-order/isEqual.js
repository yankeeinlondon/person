import { isSameTypeOf } from "src/runtime/index";
/**
 * **isEqual**(compareTo)(value)
 *
 * Higher order type guard to detect whether two values are equal
 */
export const isEqual = (base) => (value) => isSameTypeOf(base)(value)
    ? value === base
        ? true
        : false
    : false;
