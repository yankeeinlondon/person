/**
 * **sharedKeys**
 *
 * Runtime utility which provides the _shared_ keys between two objects.
 */
export const sharedKeys = (a, b) => {
    const ka = Object.keys(a);
    const kb = Object.keys(b);
    return ka.filter(k => kb.includes(k));
};
