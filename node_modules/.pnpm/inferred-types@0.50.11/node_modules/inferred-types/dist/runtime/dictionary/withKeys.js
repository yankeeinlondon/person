import { retain } from "./retain";
/**
 * **withKeys**(obj,keys)
 *
 * Reduces the key/value pairs in an object to those keys
 * explicitly stated.
 *
 * - note: this function is an alias for `retain()`
 */
export const withKeys = (dict, ...keys) => retain(dict, ...keys);
