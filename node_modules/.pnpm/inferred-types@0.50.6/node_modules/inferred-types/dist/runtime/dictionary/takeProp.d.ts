import { Narrowable, TakeProp, Something } from "src/types/index";
/**
 * **takeProp**(val, prop, otherwise)
 *
 * Takes a property from a value if it is a _container_ and "prop"
 * is a valid index of the container. If not then it returns the
 * value provided in "otherwise".
 *
 * **Related:** `get`
 */
export declare const takeProp: <T extends Something, P extends PropertyKey, E extends Narrowable>(val: T, prop: P, otherwise: E) => TakeProp<T, P, E>;
//# sourceMappingURL=takeProp.d.ts.map