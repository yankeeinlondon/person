/**
 * **withDefaults**(defaults) → (obj) → _merged_
 *
 * Merges in default values to an existing object and maintains
 * narrow typing.
 */
export const withDefaults = (with_defaults) => (obj) => {
    const merged = {
        ...with_defaults,
        ...obj
    };
    return merged;
};
