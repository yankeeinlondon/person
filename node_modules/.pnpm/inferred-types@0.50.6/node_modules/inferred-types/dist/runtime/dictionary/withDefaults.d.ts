import { MergeObjects, Narrowable, ObjectKey } from "src/types/index";
/**
 * **withDefaults**(defaults) → (obj) → _merged_
 *
 * Merges in default values to an existing object and maintains
 * narrow typing.
 */
export declare const withDefaults: <TDefaults extends Record<ObjectKey, N>, N extends Narrowable>(with_defaults: TDefaults) => <TObj extends Record<ObjectKey, W>, W extends Narrowable>(obj: TObj) => MergeObjects<Required<TDefaults>, TObj>;
//# sourceMappingURL=withDefaults.d.ts.map