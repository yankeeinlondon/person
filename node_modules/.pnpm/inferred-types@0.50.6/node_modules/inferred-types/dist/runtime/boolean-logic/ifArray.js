/**
 * **ifArray**(T, IF, ELSE)
 *
 * A utility which evaluates a type `T` for whether it is an array and then
 */
export function ifArray(val, isAnArray, isNotAnArray) {
    return (Array.isArray(val) ? isAnArray(val) : isNotAnArray(val));
}
