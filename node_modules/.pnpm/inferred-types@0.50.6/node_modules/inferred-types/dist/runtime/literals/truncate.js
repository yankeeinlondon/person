export const truncate = (content, maxLength, ellipsis = false) => {
    const overLimit = content.length > maxLength;
    return (overLimit
        ? ellipsis
            ? `${content.slice(0, maxLength)}${typeof ellipsis === "string" ? ellipsis : "..."}`
            : content.slice(0, maxLength)
        : content);
};
