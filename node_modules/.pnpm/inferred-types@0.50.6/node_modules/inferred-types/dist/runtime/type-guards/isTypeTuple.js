/**
 * **isTypeTuple**(value)
 *
 * A type-guard which detects if passed in value is a `TypeTuple`.
 */
export function isTypeTuple(value) {
    return Array.isArray(value) && value.length === 3 && typeof value[1] === "function";
}
