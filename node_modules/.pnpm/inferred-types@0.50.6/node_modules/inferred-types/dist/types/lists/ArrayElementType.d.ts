import { IsEmptyContainer, TupleToUnion, Widen } from "src/types/index";
/**
 * **ArrayElementType**`<T>`
 *
 * Determines what type the elements in an array are.
 * ```ts
 * // string
 * type T = ArrayElementType<string[]>;
 * ```
 */
export type ArrayElementType<T extends readonly unknown[]> = IsEmptyContainer<T> extends true ? [...T][0] : Widen<TupleToUnion<T>>;
//# sourceMappingURL=ArrayElementType.d.ts.map