import type { CamelCase } from "src/types/index";
/**
 * **toCamelCase**(str)
 *
 * Converts a string into `camelCase` while preserving literal strings.
 *
 * **Note:** _by default it also removes surrounding white space (if it exists) but it
 * can be preserved if you change the `preserveWhitespace` flag._
 *
 * **Related:** `toKebabCase`, `toPascalCase`, `toSnakeCase`
 */
export declare function toCamelCase<TString extends string, TPreserve extends boolean = false>(input: TString, preserveWhitespace?: TPreserve): CamelCase<TString, TPreserve>;
//# sourceMappingURL=toCamelCase.d.ts.map