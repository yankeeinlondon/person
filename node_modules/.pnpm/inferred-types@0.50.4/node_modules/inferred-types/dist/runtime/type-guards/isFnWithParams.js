/**
 * **isFnWithDict**(input)
 *
 * Type guard which checks whether a give variable is a function
 * which _also_ contains
 */
export function isFnWithParams(input) {
    return typeof input === "function" && Object.keys(input)?.length > 0;
}
