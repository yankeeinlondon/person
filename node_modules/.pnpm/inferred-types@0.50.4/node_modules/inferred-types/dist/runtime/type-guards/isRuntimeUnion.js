/**
 * **isRuntimeUnion**
 *
 * A type guard which checks whether the passed in value is a `RuntimeUnion`.
 */
export const isRuntimeUnion = (val) => {
    return typeof val === "object" && "kind" in val && val.kind === "Union" ? true : false;
};
