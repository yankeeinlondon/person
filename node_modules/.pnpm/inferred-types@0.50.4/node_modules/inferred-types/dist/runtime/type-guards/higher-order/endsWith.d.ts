import { Narrowable } from "src/types/index";
export type EndingWithTypeGuard<T extends string> = <V extends Narrowable>(val: V) => val is V & `${string}${T}`;
/**
 * **endsWith**(endingIn) => (val)
 *
 * Creates a TypeGuard which checks whether a value _ends with_ a
 * particular string literal.
 */
export declare const endsWith: <T extends string>(endingIn: T) => EndingWithTypeGuard<T>;
//# sourceMappingURL=endsWith.d.ts.map