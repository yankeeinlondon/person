import { Alpha, Narrowable } from "src/types/index";
/**
 * **isAlpha**(value)
 *
 * Type guard which ensures that the given value is a string literal with only
 * alphabetic characters.
 */
export declare const isAlpha: <T extends Narrowable>(value: T) => value is T & Alpha<T>;
//# sourceMappingURL=isAlpha.d.ts.map