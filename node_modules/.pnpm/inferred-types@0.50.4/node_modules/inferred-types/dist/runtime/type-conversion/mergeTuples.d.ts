import type { MergeTuples, Narrowable } from "src/types/index";
/**
 * **mergeTuples**(a,b)
 *
 * Replaces values in `a` with `b` unless `b` is _undefined_. Also, if
 * `a` has more elements than `b` then these extra elements will be
 * appended to the end.
 */
export declare function mergeTuples<A extends readonly Narrowable[], B extends readonly Narrowable[]>(a: A, b: B): MergeTuples<A, B>;
//# sourceMappingURL=mergeTuples.d.ts.map