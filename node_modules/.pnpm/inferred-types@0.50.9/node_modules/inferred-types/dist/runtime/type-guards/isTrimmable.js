import { isString } from "./isString";
/**
 * **isTrimable**`(val)`
 *
 * tests whether a string has whitespace on either left or right edges.
 */
export const isTrimable = (val) => {
    return isString(val) && val !== val.trim();
};
