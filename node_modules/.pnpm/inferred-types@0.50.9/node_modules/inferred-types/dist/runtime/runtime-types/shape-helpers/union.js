/**
 * **union**
 *
 * provides API implementation for defining a union
 */
export const union = (...elements) => {
    const result = elements.map(_el => {
        // TODO
    });
    return result;
};
