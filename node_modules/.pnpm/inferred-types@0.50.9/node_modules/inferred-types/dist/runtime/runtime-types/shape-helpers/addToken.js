/**
 * Adds any `TypeToken` by receiving the `TypeTokenKind` and then whatever
 * parameters that type requires.
 */
export const addToken = (token, ...params) => `<<${token}${params.length > 0 ? `::${params.join("::")}` : ""}>>`;
